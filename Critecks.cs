using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Critecks : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Crickecks");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
